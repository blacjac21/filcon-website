import { AppBar, Toolbar } from "@mui/material";
import React from "react";

const Navbar = () => {
    return (
        <AppBar>
            <Toolbar>
              NavBar
            </Toolbar>
        </AppBar>
    )
}

export default Navbar